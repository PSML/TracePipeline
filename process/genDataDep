dirList = glob("bmps/0*");

#{
dirList{1,1}
imread(dirList{1,1})
size(imread(dirList{1,1}));
#}
Z = zeros(size(imread(dirList{1,1})));


for i = 1:length(dirList)
    name = dirList{i,1};
    Z += imread(name);
end

data_out = Z > 0 & Z < length(dirList);

imwrite( data_out, "data_dep.bmp");